name: Vault Integration

on:
  workflow_dispatch: 
  push:
    branches: [ main ]

permissions:
  id-token: write   # REQUIRED for OIDC
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Vault
        uses: hashicorp/vault-action@v3
        with:
          url: https://8200-port-l6nxqi3mc3fzz2mq.labs.kodekloud.com/
          method: jwt
          role: github
          secrets: |
            kv/data/github/db username | DB_USER ;
            kv/data/github/db password | DB_PASS

      - name: Use secrets
        run: |
          echo "DB_USER=$DB_USER"


